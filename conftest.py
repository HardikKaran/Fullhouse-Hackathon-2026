# Presence of this file puts the repo root on sys.path under pytest's default
# (prepend) import mode, so `from harness import ...` resolves without installing
# the package. harness.paths then bootstraps the vendored engine.
