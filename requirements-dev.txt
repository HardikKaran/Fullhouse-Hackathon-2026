# Dev/harness deps. Mirrors the tournament sandbox image so local == prod.
# NOTE: eval7 is installed separately by scripts/setup_env.sh (it needs the
# Cython<3 + --no-build-isolation two-step and would fail a plain install here).
numpy>=1.26.0
scipy>=1.13.0
treys==0.1.8
scikit-learn>=1.5.0
flask>=3.0.0
pytest
